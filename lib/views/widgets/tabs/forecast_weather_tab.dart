import 'package:flutter/material.dart';

class ForecastWeatherTab extends StatelessWidget {
  const ForecastWeatherTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
